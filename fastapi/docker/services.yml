# This configuration is intended for development purpose, it's **your** responsibility to harden it for production
name: frsf
services:
  keycloak:
    extends:
      file: ./keycloak.yml
      service: keycloak
  jhipster-registry:
    extends:
      file: ./jhipster-registry-with-kp.yml
      service: jhipster-registry
    depends_on:
      keycloak:
        condition: service_healthy
